class DBAccessor:

    @staticmethod
    def get_cost(pizza_name: str) -> float:
        # access pizza cost from db
        pizza_cost: float = 0.0
        return pizza_cost

    @staticmethod
    def get_pizza_base_cost(pizza_base_name: str) -> float:
        # access pizza base cost from db
        pizza_base_cost: float = 0.0
        return pizza_base_cost

    @staticmethod
    def get_cost_of_toppings(topping_name: str) -> float:
        # access pizza topping cost from db
        pizza_topping_cost: float = 0.0
        return pizza_topping_cost
